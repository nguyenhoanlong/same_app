<li>
  <%= gravatar_for user, size: Settings.size_avt %>
  <%= link_to user.name, user %> | 
  <% if current_user.admin? && !current_user?(user) %>
    <%= link_to (t "users.index.delete"), user, method: :delete,
      data: {confirm: (t "users.index.confirm")} %>
  <% end %>
</li>
